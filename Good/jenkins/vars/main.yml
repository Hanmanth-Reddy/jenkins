---
  my_jenkins_plugin:
            ant: enabled
            maven: enabled
            git: enabled
            ace-editor: enabled
            ant: enabled
            apache-httpcomponents-client-4-api: enabled
            authentication-tokens: enabled
            bouncycastle-api: enabled
            branch-api: enabled
            build-timeout: enabled
            cloudbees-folder: enabled
            antisamy-markup-formatter: enabled
            command-launcher: enabled
            credentials: enabled
            credentials-binding: enabled
            display-url-api: enabled
            docker-commons: enabled
            docker-workflow: enabled
            durable-task: enabled
            email-ext: enabled
            git: enabled
            git-client: enabled
            github: enabled
            github-api: enabled
            github-branch-source: enabled
            git-server: enabled
            gradle: enabled
            handlebars: enabled
            jackson2-api: enabled
            jquery-detached: enabled
            jsch: enabled
            junit: enabled
            ldap: enabled
            mailer: enabled
            mapdb-api: enabled
            matrix-project: enabled
            matrix-auth: enabled
            momentjs: enabled
            pam-auth: enabled
            pipeline-build-step: enabled
            pipeline-github-lib: enabled
            pipeline-graph-analysis: enabled
            pipeline-input-step: enabled
            pipeline-milestone-step: enabled
            pipeline-model-api: enabled
            pipeline-model-declarative-agent: enabled
            pipeline-model-definition: enabled
            pipeline-model-extensions: enabled
            pipeline-rest-api: enabled
            pipeline-stage-step: enabled
            pipeline-stage-tags-metadata: enabled
            pipeline-stage-view: enabled
            plain-credentials: enabled
            resource-disposer: enabled
            scm-api: enabled
            script-security: enabled
            ssh-credentials: enabled
            ssh-slaves: enabled
            structs: enabled
            subversion: enabled
            timestamper: enabled
            token-macro: enabled
            windows-slaves: enabled
            workflow-aggregator: enabled
            workflow-api: enabled
            workflow-basic-steps: enabled
            workflow-cps: enabled
            workflow-cps-global-lib: enabled
            workflow-durable-task-step: enabled
            workflow-job: enabled
            workflow-multibranch: enabled
            workflow-scm-step: enabled
            workflow-step-api: enabled
            workflow-support: enabled
            ws-cleanup: enabled
